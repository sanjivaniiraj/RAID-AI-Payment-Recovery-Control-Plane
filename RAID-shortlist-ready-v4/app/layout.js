import './globals.css';

export const metadata = {
  title: 'RAID — Payment Recovery Control Plane',
  description: 'AI-powered payment state reconstruction and safe recovery decisions'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
